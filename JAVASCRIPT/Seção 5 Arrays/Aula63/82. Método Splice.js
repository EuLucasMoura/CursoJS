const nomes = ['Maria', 'Lucas', 'Victória', 'Pedro'];

// Unshift

nomes.splice(2, 0, 'Luiz', 'Otávio');
console.log(nomes);

//nomes.splic(índice atual , delete, elem1, elem2, elem3);
// pop
// const removidos = nomes.splice(-1, 1)

//shift
//const removidos = nomes.splice(0, 1)